module rf{
    export interface IParticleNodeInfo{
        
    }

    export interface iTimeParticleNodeInfo{
        useDuration:boolean;
        useLooping:boolean;
        useDelay:boolean;

        
    }
}